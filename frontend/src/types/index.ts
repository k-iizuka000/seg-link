export interface Template {
  id: string;
  userId: string;
  name: string;
  clothing: string;
  wheels: string;
  wind: string;
  weight: number;
  notes?: string;
  createdAt: string;
  updatedAt: string;
} 